{
  "name": "stat-services",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npx nodemon --exec \"npx tsx server.ts\"",
    "db:push": "npx drizzle-kit push"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "tsx": "^4.7.0"
  }
}
