{
  "name": "auth-services",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npx nodemon --exec \"npx tsx server.ts\"",
    "db:push": "npx drizzle-kit push"
  },
  "dependencies": {
    "bcrypt": "^6.0.0"  }
}
