{
  "name": "kanban-services",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "root/services/auth-service",
    "root/services/workspace-service"
  ],
  "scripts": {
    "auth:dev": "npm run dev --workspace=root/services/auth-service",
    "auth:migrate": "npm run db:push --workspace=root/services/auth-service",
    "workspace:dev": "npm run dev --workspace=root/services/workspace-service",
    "workspace:migrate": "npm run db:push --workspace=root/services/workspace-service",
    "dev": "npm run auth:dev & npm run workspace:dev"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
