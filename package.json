{
  "name": "kanban-services",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "root/services/**"
  ],
  "scripts": {
    "auth:dev": "npm run dev --workspace=root/services/auth-service",
    "auth:migrate": "npm run db:push --workspace=root/services/auth-service",
    "stat:dev": "npm run dev --workspace=root/services/statistic-service",
    "stat:migrate": "npm run db:push --workspace=root/services/statistic-service"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
